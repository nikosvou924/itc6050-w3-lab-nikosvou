## Summary: When SQL, when MongoDB, when both?

PostgreSQL excels at complex analytical queries with joins, aggregations, and window functions. The syntax is clear readable. MongoDB shines when data is naturally document-shaped, when schema flexibility is needed, or when embedding related data (like order items) avoids expensive joins. For an e-commerce analytics use case , SQL was consistently clearer and faster to write. 

